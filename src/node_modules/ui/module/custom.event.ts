export default class CustomEvent {
    public name: string;
    public payload: object;

    public constructor(config) {
        this.name = config.name;
        this.payload = config.payload != null ? config.payload : null;
    }
}